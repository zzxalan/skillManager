interface:
  display_name: "Replace With Skill Name"
  short_description: "Replace with a short user-facing summary."
  default_prompt: "Use $replace-with-skill-name to complete this workflow."

policy:
  allow_implicit_invocation: true
