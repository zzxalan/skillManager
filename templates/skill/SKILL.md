---
name: replace-with-skill-name
description: Replace this with a concrete description of what the skill does and when to use it. Include likely trigger words, file types, systems, or workflows.
---

# Replace With Skill Name

## Goal

State the specific outcome this skill should produce.

## Workflow

1. Confirm the required inputs and locate relevant files or systems.
2. Follow the team-specific steps in order.
3. Use bundled resources only when they are relevant.
4. Produce the final output in the expected format.

## Resources

- Read `references/example.md` only when detailed rules or examples are needed.
- Run scripts from `scripts/` for deterministic parsing, conversion, or validation.
- Use files from `assets/` as templates or static resources for generated output.

## Output

Describe the expected structure, tone, file format, or acceptance criteria.

## Edge Cases

- If required input is missing, ask for the smallest missing detail or state the assumption.
- If a tool or system is unavailable, report the blocker and provide the best partial result.
- If the task touches production data, secrets, or irreversible operations, ask for explicit confirmation.
