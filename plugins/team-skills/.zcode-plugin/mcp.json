{
  "mcpServers": {
    "smart-cicd": {
      "type": "http",
      "url": "${user_config.mcpUrl}",
      "headers": {
        "X-CICD-Username": "${user_config.username}",
        "X-CICD-Password": "${user_config.password}"
      }
    }
  }
}
