interface:
  display_name: "Smart Frontend Standards"
  short_description: "用于 smart 项目前端页面、H5、微前端与前端审查规范。"
  default_prompt: "Use $smart-frontend-standards to 处理这个 smart-* 插件前端任务，并按前端规范选择 references。"

policy:
  allow_implicit_invocation: true
